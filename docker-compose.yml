services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: ha_memory
      POSTGRES_USER: hamem
      POSTGRES_PASSWORD: hamem
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
